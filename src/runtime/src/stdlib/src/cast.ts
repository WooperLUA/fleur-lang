import {    type NumberValue,    RuntimeValueType, type StringValue,} from "@types";import {    throw_exception,    check_args_length,    check_arg_type, stringify_value} from "@utils";import type {    RuntimeValue,    NativeFunctionValue,    StructValue,} from "@types";export const cast: StructValue = {    type:       RuntimeValueType.Struct,    identifier: "cast",    properties: new Map<string, RuntimeValue>(),    methods:    new Map<string, any>([        [            "to_num",            {                type: RuntimeValueType.NativeFunction,                call: (args: RuntimeValue[]) =>                      {                          check_args_length(args, 1, "cast::to_num");                          const value = (args[0] as any).value                          const num = Number(value);                          return {type: RuntimeValueType.Number, value: Number.isNaN(num) ? 0 : num};                      },            } as NativeFunctionValue,        ],        [            "to_str",            {                type: RuntimeValueType.NativeFunction,                call: (args: RuntimeValue[]) =>                      {                          check_args_length(args, 1, "cast::to_str");                          return {type: RuntimeValueType.String, value: stringify_value(args[0])};                      },            } as NativeFunctionValue,        ],        [            "to_bool",            {                type: RuntimeValueType.NativeFunction,                call: (args: RuntimeValue[]) =>                      {                          check_args_length(args, 1, "cast::to_bool");                          const value = (args[0] as any).value                          return {type: RuntimeValueType.Boolean, value: Boolean(value)};                      },            } as NativeFunctionValue,        ],    ]),};